version: '3'
services:
  app:
    build: .
    image: template-app:1.0
    command: ["java", "-Xmx1g", "-agentlib:jdwp=transport=dt_socket,server=y,suspend=n,address=5005", "-Dspring.profiles.active=dev", "-jar", "sample-app.jar" ]
    - "8080:8080"
